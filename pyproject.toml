[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "adfler"
version = "0.1.0"
description = "Advanced Drone Flight Log Entity Recognizer"
readme = "README.md"
authors = [
  { name = "Your Name", email = "your.email@example.com" },
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
requires-python = ">=3.7"
dependencies = [
    "simpletransformers>=0.63.6",
    "seqeval>=1.22.0",
    "pandas",
    "numpy",
    "torch",
    "tqdm",
    "pdfkit",
    "requests",
]

[project.scripts]
adfler = "adfler.adfler:main"

[project.urls]
"Homepage" = "https://github.com/DroneNLP/adfler"
"Bug Tracker" = "https://github.com/DroneNLP/adfler/issues"

[tool.setuptools.packages.find]
where = ["."]
include = ["adfler*"]
namespaces = false
